/**
 * @deepseek-ai/dsh-multi-agent-app — the multi-agent browser profile's static
 * patch layer. The package selects the orchestration preset and mounts the
 * official Codex and Claude Code subagent providers; it has no runtime API.
 * @module @deepseek-ai/dsh-multi-agent-app
 */

export {}
